# 推广软件 · 整体架构设计与长期演进规划

> 文档角色：架构决策基线（ADR 合集 + C4 分层图 + 演进路线图）
> 适用阶段：绿地项目从 0 启动，目标 1–3 年可平滑演进
> 作者视角：软件架构师（领域驱动 · 权衡优先 · 可逆性优先）

---

## 0. 设计前先对齐的事实（约束）

| 维度 | 结论 | 对架构的影响 |
|---|---|---|
| 业务定位 | 营销/用户增长平台（拉新、促活、裂变、权益发放） | 领域边界 = 用户/活动/权益/触达/规则/分析 |
| 系统现状 | 全新绿地，无历史负担 | 技术选型自由，可按最佳实践起手 |
| 扩展驱动 | 高并发/高可用 **+** 数据/智能分析 **+** 业务快速变化（三者皆要） | 架构需同时化解三股压力，但不能靠堆服务 |
| 团队现实 | ≤10 人小团队 | 优先"可维护"，避免过早微服务化 |

---

## 1. 核心架构决策（关键权衡）

**起点：模块化单体（Modular Monolith）+ 预留服务提取接缝。**

为什么不是直接微服务：
- 微服务对小团队的隐性成本（分布式事务、链路治理、独立部署运维、调试复杂度）远高于其收益；在团队 < 50 人、领域边界尚不稳定时，模块化单体性价比最高。

为什么不是"大泥球"单体：
- 以 DDD **限界上下文**划分**强隔离模块**；模块间只允许通过"事件总线 / 接口契约"通信。
- 物理上同进程、逻辑上已是未来服务——后续拆分是"搬运"而非"重写"。

三大驱动如何不靠微服务就能化解：
- 高并发 → 无状态水平扩展 + 缓存 + 限流降级
- 数据/分析 → CQRS 读写分离 + CDC 到 OLAP，分析负载不回压生产库
- 业务多变 → 规则引擎 + 配置中心 + 插件化，玩法可配置不硬编码

---

## 2. 分层与模块（C4 容器/组件级）

```
客户端(Web/App/小程序/开放API)
        │
   API 网关 / BFF（鉴权·限流·路由）
        │
┌────────────────────── 模块化单体应用（强隔离模块 · 共享进程）──────────────────────┐
│  用户与身份 │ 增长活动 │ 权益激励 │ 触达通知 │ 规则/配置 │ 数据分析(写侧)          │
│   └─ 模块间仅通过 内部事件总线 / 接口契约 通信 ─┘                                  │
└──────────────────────────────────────────────────────────────────────────────┘
        │                                          │
   基础设施（共享）                              数据分析面（CQRS 读侧）
   主库(写) / 只读从库(读)                        CDC 同步 → OLAP/数仓(ClickHouse)
   Redis 缓存 / 配置中心 / 内部事件总线             → 实时+离线报表 · 归因 · 算法特征
```

**技术分层（单进程内）**：
API/BFF → 应用服务（用例编排）→ 领域层（聚合 + 领域事件）→ 基础设施（持久化/消息/缓存）
跨切面：可观测性（日志/指标/链路）、配置中心、事件总线。

**限界上下文清单**：
1. 用户与身份：注册登录、画像、会员等级
2. 增长活动：拉新/促活/裂变活动的配置与执行
3. 权益激励：积分、券、红包的发放与核销
4. 触达通知：短信 / 推送 / 站内信
5. 规则与配置：活动规则可配置化、策略插件
6. 数据分析（写侧）：埋点采集、领域事件产出

---

## 3. 三大驱动的具体化解法

### 3.1 高并发 / 高可用
- 应用**无状态**，多实例水平扩展；网关层令牌桶限流、熔断、降级
- Redis 缓存热点数据（用户画像、活动配置、权益余额）；静态资源走 CDN
- 数据库**读写分离**（写主读从），连接池统一管控
- 大促预案：队列削峰 + 非核心功能降级（如实时榜单降为近似）

### 3.2 数据 / 智能分析
- **CQRS**：写侧产出领域事件，读侧构建物化视图，读写模型解耦
- **CDC**（如 Debezium）将变更同步至 OLAP（ClickHouse）/ 数仓，与生产库**物理隔离**
- 实时（Kafka + 轻量聚合）+ 离线（批处理）双管道
- 分析负载绝不回压生产事务库——这是高并发与分析压力能共存的关键

### 3.3 业务快速变化
- **规则引擎 + 可视化配置**：活动玩法可组装，不硬编码
- **策略模式 / 插件机制**：新玩法以插件接入核心，不动主干
- **配置中心**（Nacos/Apollo）管理开关、阈值、渠道参数
- 权益与触达**模板化**，运营自助配置

---

## 4. 架构决策记录（ADR 摘要）

| ADR | 决策 | 主要权衡（得到 / 放弃） |
|---|---|---|
| ADR-001 | 模块化单体而非微服务起步 | 得到：低运维、快迭代；放弃：独立扩缩容（暂不需要） |
| ADR-002 | CQRS + 读写分离应对分析压力 | 得到：分析不拖累生产；放弃：写读短暂延迟（最终一致） |
| ADR-003 | 内部事件总线作为解耦与拆分接缝 | 得到：模块解耦 + 未来零改业务代码换 Kafka；放弃：调试需看事件流 |
| ADR-004 | 规则引擎/配置化应对业务多变 | 得到：玩法上线快；放弃：配置错误风险（需校验+回滚） |
| ADR-005 | 缓存 + 限流 + 降级保障高并发 | 得到：扛峰值；放弃：数据可见性有秒级延迟（需产品告知） |

---

## 5. 长期演进路线图（可逆、按信号拆分）

| 阶段 | 时间 | 目标 | 关键交付 |
|---|---|---|---|
| 阶段 0 | 0–3 月 | 模块化单体 MVP | 核心上下文落地 + 事件总线基线 + 读写分离基线 |
| 阶段 1 | 3–9 月 | 配置化 + 数据管道成熟 | 规则引擎/配置中心上线；CDC→OLAP 跑通；缓存/限流体系化 |
| 阶段 2 | 9–18 月 | 首个服务拆分 | 出现"拆分信号"时，用**绞杀者模式**提取首个服务（通常高独立/高负载的权益或触达） |
| 阶段 3 | 18 月+ | 多服务 / 推广中台 | 服务编排与治理，沉淀可复用推广能力中台 |

**拆分信号（何时该拆，而非按时间表硬拆）**：
团队扩张到 >15 人且按域划分、某模块有独立扩缩容需求、技术栈分化、部署频率相互冲突、某模块故障域需隔离。**任一出现即拆，且只拆该模块**——其余继续留在单体，避免"全有或全无"的拆分风险。

---

## 6. 风险与护栏

- **最大风险**：模块化单体的模块边界被悄悄打破（直接跨模块查库）。
  **护栏**：编译期模块依赖校验、事件契约测试、定期架构评审。
- **过度设计风险**：不要为"未来可能"预先重架构（YAGNI）；接缝预留即可，不必提前实现。
- **一致性风险**：缓存与最终一致带来秒级延迟，产品侧需明确告知用户（如"权益到账可能有数秒延迟"）。

---

## 7. 给团队的首步行动

1. 先定**模块边界与接口契约**（哪怕先用单体，边界先行）
2. 选**事件总线**（内部内存队列起步，后期换 Kafka 零改业务代码）
3. 立 **CDC→OLAP** 数据基线，分析能力从第一天就独立
4. 建**配置中心 + 规则引擎**最小可用版，让运营能自助配置
