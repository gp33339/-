# 真实数据源配置（复制为 .env 并填值；.env 已被 .gitignore 忽略，不会进仓库）
# 不填任何 key 时，引擎自动回退到内置演示数据（36 家样本投影）。

# 企查查开放平台（国内工商）：https://openapi.qcc.com  → 企业实名 + 开通"企业模糊搜索(886)"
QCC_APPKEY=你的AppKey
QCC_SECRET=你的SecretKey

# Hunter.io（海外公司发现 + 邮箱补全）：https://hunter.io/api/v1/docs
# 填 'test-api-key' 可零成本验证整条链路（返回示例数据，不耗额度）；
# 填你账户的真实 key 即启用 discover 按行业/国家搜真实公司。
HUNTER_API_KEY=test-api-key

# 真 key 下，是否对 discover 命中的公司再补全邮箱（会消耗 Hunter 额度）
ENRICH_EMAILS=0

# 海关数据 API（阀门外贸最强采购信号源：进口商提单记录）
# 留空 → 使用内置阀门进口商演示样本（默认即可看到"海关进口"信号生效）。
# 填真实 key（腾道 tendata.cn 等，需签合同获取 endpoint + token）→ 启用真实提单查询。
CUSTOMS_API_KEY=
# 真实 API 基址（签合同后由服务商提供；留空用默认占位，仅真实 key 时生效）
CUSTOMS_API_BASE=
